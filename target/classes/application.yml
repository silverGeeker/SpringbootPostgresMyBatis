server:
  port: 5000
spring:
  application:
    name: Postgres-Demo
  datasource:
    driver - class- name: org.postgresql.Driver
    jdbc-url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: root